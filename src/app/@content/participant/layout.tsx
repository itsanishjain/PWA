export const metadata = {
    title: 'Participant',
    description: 'Participant layout',
}

export default function ParticipantLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>
}
